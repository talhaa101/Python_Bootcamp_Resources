#!/usr/bin/env python3


def odd_or_even(number):
    if number % 2 == 0:
        return 'Even'
    else:
        return 'Odd'

#odd_or_even_string = odd_or_even(8)
#  
#odd_or_even(5)  # this will return 'Odd' but we are not doing anything with it yet
print(odd_or_even(5))

#odd_or_even_string = odd_or_even(11)
#print(odd_or_even_string)

